#include <iostream>
namespace std

int suma(int a, int b){
	return a + b;
	return 0;
}


int main() {
	// Variables
	int(tipo de valor que puedo guardar) variable; 
	float variable decimal = 0.0;
	double variableDecimalLarga;
	variableDecimalLarga = 0.0;
	
	// Arrays
	int arrayEntero[12];
	double arrayDecimal[5] = {2,3,4,5,3,4,5,6,4,3};
	
	// Interacion con usuario
	cout << "Un texto " << variableEntera << "Otro texto "<< endl;
	cin >> userDatos;
	
	// Condicional
	if(userDatos > variableEntera) {
		// Se hace esto si la primera condicion es cierta.
	}else if (userDatos < -50){
		// Se hace esto si todas las condiciones NO son ciertas.
		// y la segunda es cierta 
}   else {
	 // Se hace esto si
	 
	 
	 switch (userDatos)
	 case 0:
	 	case 1:
	 		// SE hace esto si user datos es 0 o 1
	 		break;
	 		case 2: 
	 		// se hace esto si userDatos es 2 
	 		break;
	 		deffault:
	 			// Se hace esto si userDatos NO es 0,1 o 2
	 		break;
		}
	 			
	// Recorrer arrays
	for (int i=0; 1 < 5; i++){	
	cout << arrayDecimal[i] << 
} 
}
	 	
}
		
		
	}
		
		
		
	}
	
	
	return 0;
}

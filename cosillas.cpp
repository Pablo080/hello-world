#include <iostream>

using namespace std
    
int main (){
	int numEntero1; // Creo una variable entera.
	float numReal; // Creo una variable real.
	numEntero1= 0; // Doy valor a una variable.
	
	
	// Arrays
	int numEnteros [5] ; // Creo un array con cinco posiciones
	int numEnteros = {10, 30, 2, 69, 43} // Damos un valor al array  
	
	
	cout << "Dame un numero entero: ";
	cin >> numEntero1;
	if (numEntero<100){
		//Si es cierto hago esto
}   else {
	// Si ninguno de los es cierto hago esto
} 
    switch (numEntero1){
    	case0: //Hago algo si es 0
    	break;
    	case1: //Hago algo si es 1
    	break;
    	case2: 
		case3:
		case4://Hago algo si es 2
		break;
		deffault: //Hago algo si no es ninguno anterior
	}
    //Bucles 
	int contador = 0;
	while (contador < 10){
		//Mientras eso sea cierto, voy a hacer eso.
		contador++; 
	} 
	
    do{
    	contador++;
    } while (contador < 10);
    
    for (int i=0; i=0; i<=10; i++)
    	//Haz esto, como maximo, mas de 10 veces
    	
	}
	return 0;
}
